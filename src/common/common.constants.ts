export enum Containers {
	/** Container names will contain the world id. `world${uuid}` */
	World = 'world'

}

export enum ContainerFolders {
	DebugErrors = 'debug/errors',
	DebugInfos = 'debug/info',
	DebugWarnings = 'debug/warnings',
	DebugDebugs = 'debug/debugs'
}

export enum Tables {
	Worlds = 'worlds'
}

export enum EnvVar {
	Storage = 'STORAGE_CONNECTION'
}